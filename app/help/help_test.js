'use strict';

describe('myApp.help module', function() {

  beforeEach(module('myApp.Help'));

  describe('help controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var helpCtrl = $controller('HelpCtrl');
      expect(helpCtrl).toBeDefined();
    }));

  });
});